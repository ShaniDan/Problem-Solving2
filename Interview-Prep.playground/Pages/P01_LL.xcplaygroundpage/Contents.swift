//: [Previous](@previous)

import Foundation

var greeting = "Hello, playground"

//: [Next](@next)

// Find the last element of a linked list.

class List<T> {
    var value: T
    var next: List<T>?
    
    init(value: T, next: List<T>? = nil) {
        self.value = value
        self.next = next
    }
}


extension List {
    var last: T? {
        
    }
    return nil
}
